# robotvision
Machine Learning @ Purdue - Robot Vision Team (2025-2026)


Train a VAE to compress the output of the model.
Use a VAE to compress the input of the images
Use multiple encoders
Use a symphony of models.
Create a model that will predict future displacement of objects
Lane segmentation
Depth extraction, object detection, VAEs dimensional reduction, sobel filters, multiple vision Transformers. 

