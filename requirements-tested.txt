# Exact versions the paper artifacts were produced with (DeltaAI, Python 3.12, CUDA 13, aarch64).
# `uv pip install -e .` resolves fresh versions; use this file to match the tested environment:
#   uv pip install -r requirements-tested.txt -e .
torch==2.11.0
torchvision==0.26.0
pytorch-lightning==2.6.1
timm==1.0.26
transformers==5.6.0
numpy==2.4.6
pandas==3.0.2
matplotlib==3.10.9
pillow==12.2.0
protobuf==6.33.6
tqdm==4.67.3
wandb==0.26.1
huggingface-hub==1.12.2
# visual-edits extra
openai==2.6.1
python-dotenv==1.2.2
ultralytics==8.4.43
vllm==0.20.1
